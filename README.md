# MySQL-progress
create database, use database, creaate tables, work with tables, insert, update, delete, rename, alter, rollback, commit, set autocommit, set sql safe updates, creating and working on tables with column names and assigning datstypes, add, drop, where and functions such as current_date(), now(), current_time() 
